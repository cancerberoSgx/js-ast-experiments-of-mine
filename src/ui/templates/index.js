// const fs = require('fs')
// const path = require('path')

// const names = ['exampleList.hbs', 'layout.hbs']
// const templates = names.map(name => ({ name, content: fs.readFileSync(path.join(__dirname, 'exampleList.hbs')).toString() }))

// const handlebars = require('handlebars')
// const renderTemplate = (name, context) =>
//   handlebars.compile(templates.find(t=>t.name===name).content)(context)

// module.exports = {templates, renderTemplate}
// console.log(templates);
